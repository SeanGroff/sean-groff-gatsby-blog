import React, { memo } from 'react'

import ghost from '../assets/ghost_emoji.png'

const IconGhost = memo(() => {
  return <img alt="ghost emoji" src={ghost} width={16} height={16} />
})

export default IconGhost
